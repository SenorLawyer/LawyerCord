name: Release

on:
    push:
        tags:
            - "v*"

permissions:
    contents: write

jobs:
    release:
        name: Build release
        runs-on: windows-latest
        timeout-minutes: 30

        steps:
            - name: Check out repository
              uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7
              with:
                  persist-credentials: false

            - name: Set up pnpm
              uses: pnpm/action-setup@0ebf47130e4866e96fce0953f49152a61190b271 # v6

            - name: Set up Node.js
              uses: actions/setup-node@249970729cb0ef3589644e2896645e5dc5ba9c38 # v6
              with:
                  node-version: 24
                  cache: pnpm

            - name: Install dependencies
              run: pnpm install --frozen-lockfile

            - name: Validate tag version
              shell: pwsh
              run: |
                  $packageVersion = (Get-Content -LiteralPath package.json | ConvertFrom-Json).version
                  if ($env:GITHUB_REF_NAME -ne "v$packageVersion") {
                      throw "Tag $env:GITHUB_REF_NAME does not match package version v$packageVersion"
                  }

            - name: Build and test
              run: |
                  pnpm testTsc
                  pnpm testDiscordMcp
                  pnpm testControlPanel
                  pnpm buildStandalone
                  pnpm buildWebStandalone

            - name: Package build output
              shell: pwsh
              run: Compress-Archive -Path dist\* -DestinationPath "LawyerCord-$env:GITHUB_REF_NAME.zip"

            - name: Create GitHub release
              env:
                  GH_TOKEN: ${{ github.token }}
              shell: pwsh
              run: gh release create "$env:GITHUB_REF_NAME" "LawyerCord-$env:GITHUB_REF_NAME.zip" --verify-tag --generate-notes --title "LawyerCord $env:GITHUB_REF_NAME"
