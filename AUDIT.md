# Project audit working report

This audit is still in progress. File coverage records review work; it does not establish that every finding is resolved or that the client is ready to release.

## Scope and source

- Baseline: `6e664e03ba3d0b7746ce34740ca444f5112b04bc`, published as `nightly-20260905-1918-6e664e03`.
- The baseline inventory contains 1,661 tracked files, including source, tests, configuration, documentation, and assets. The review ledger records all baseline entries as reviewed.
- At `791813b3c`, the branch contains 1,657 tracked files, with thirteen additions, seventeen removals, and 508 modifications relative to the baseline. Main contributes four automation modules, one test, and one benchmark; all six were re-read on September 24. The additional shared preset validator was reviewed with its import and storage callers.
- The five added tests cover startup flags, CRX conversion, extension installation, file selection, and settings synchronization. Their file hashes were rechecked against the reviewed versions at `480d81f23`.
- The generated internationalization mapping was checked semantically against the runtime hash function across 19,440 pairs. That is generated-data validation, not manual review of each pair.
- Main at `8fc182ba7` was reviewed separately. A fresh fetch and release lookup on September 24 confirmed that main and the published nightly remain unchanged. The nightly tag still resolves to the baseline commit. Main changes were integrated on September 24, preserving the audit version and main release history.

Changes are accumulated in [PR #47](https://github.com/SenorLawyer/LawyerCord/pull/47). The PR remains a draft with `release:nightly`; merge and auto-merge are paused. The September 24 integration resolves the conflicts with main.

## Approach and changes

The audit prioritizes deletion, then simplification, then optimization. Code is left unchanged when a proposed improvement lacks a concrete justification. Relevant callers and history are checked before shared behavior changes.

The accumulated changes cover storage consistency, asynchronous lifecycle handling, plugin behavior, native boundaries, and build/release tooling. Recent scheduling work includes:

- Retaining attempted messages after uncertain or failed sends instead of automatically replaying them.
- Letting the owning account explicitly retry a retained attempted message from the queue, while suppressing stale results after an account switch.
- Requiring every attachment upload to succeed before sending.
- Recording the originating account and enforcing it for sending and preview creation.
- Pausing older entries without a trustworthy account rather than assigning the active account.
- Stopping stale work across shutdown, logout, and reconnection.
- Preserving newer composer drafts and uploads after scheduling.
- Serializing queue reads and mutations, validating storage before the first write, and exposing changes in memory only after storage commits.
- Preserving invalid stored queues and blocking writes until a valid reload.
- Removing duplicate reaction state and preventing delayed previews from restoring removed or outdated entries.
- Rendering video previews without an extra seek, with cleanup on success, failure, and timeout.
- Preserving queue entries and previews after failed deletion, with failure feedback in the UI.
- Removing unused rescheduling and channel-filter helpers.

ThemeLibrary changes remove duplicate filtering state, cancel closed-tab requests, and lock like actions before authorization. Native downloads validate IDs and filenames, bound response size and duration, reject redirects, and replace installed files only after a temporary write succeeds. Live provider testing corrected an inaccurate string-only ID declaration.

The development-only userplugin installer now propagates installation and update preparation failures, stops on missing or malformed metadata, handles Git launch failures, and checks Git results before building updates. Review closure, page-load failures, cancellation cleanup, and external-link failures now settle the pending operation. Cleanup revalidates directories; clone destinations are explicit; review links are restricted to repository URLs. Install, update, and uninstall reject overlapping mutations within the same native instance, holding ownership through review, build, and cleanup settlement. Background update scans wait for mutations and skip directories removed before scanning. Git fetch disables terminal and Git Credential Manager prompts. External filesystem changes, separate native instances, custom credential helpers, and live Electron behavior still require review.

New installations clone into hidden staging directories excluded from plugin discovery and native builds. Replacement requires confirmation after review, preserves the installed source until promotion, and attempts to restore it if promotion fails. Replacing native code with a renderer-only plugin still requires restart. Isolated real-filesystem fixtures verified fresh installation, replacement, cancellation, and rollback after an injected promotion failure; an actual local Git clone verified the empty staging-directory boundary. These fixtures do not install or modify user plugins. Process crashes, rollback failures, and failed builds after promotion remain open.

Review templates use typed file imports and substitute placeholders in one pass, preserving literal metadata. Warning styles render directly in the stylesheet; the custom staging element and runtime style-copy step were removed.

Installer UI changes preserve existing settings when enabling unloaded plugins, render installed plugins absent from the running bundle, honor direct custom channel allowlists, and handle cancellation without treating it as an error. Duplicate update-name state and sorting work were removed. The uninstall control uses the shared button with an accessible name and consistent sizing.

The installer parses metadata using the existing TypeScript dependency without executing plugin code. Its actual handler accepts all 379 checked plugin declarations, including four rejected by the old regex. Development-only native plugins are excluded from ordinary release builds, keeping the parser out of those bundles; development and reporter builds retain it.

Native plugin discovery now applies the renderer target restrictions in desktop builds. Actual temporary plugins with `.web`, `.discordDesktop`, `.vesktop`, and `.equibop` suffixes verified matching renderer and native inclusion in both desktop targets. Those fixtures were removed and the output rebuilt without them.

Notification changes remove unused toast close callbacks, clear toasts on session changes, subscribe visible toasts to display settings, and enforce reduced queue limits on arrival. Shared notifications and toasts use separate action and dismiss controls alongside message content, with accessible labels and keyboard focus outlines. Their timers pause while keyboard focus remains inside the card. Guild toasts use Discord's resolved notification level and the raw message mention list rather than reconstructing settings and searching message text. Isolated fixtures cover these behaviors; live Discord, screen-reader acceptance, role/everyone mentions, and remaining mute-policy questions are still open.

Narrator changes preserve saved voice preferences while voices load, refresh the voice picker on browser events, retain unfamiliar language tags, and remove stale availability warnings and the English-only sample restriction. Speech formatting preserves placeholder text inside names. Announcements use the voice channel's server for nicknames, tolerate unavailable speech support, and initialize from an existing voice connection. Tests exercise actual helpers and components with mocked speech, stores, and React hooks; live playback, session transitions, and speech queue ownership remain open.

VoiceStats now rejects stale startup reads before changing totals, waits for storage before tracking, keeps failed saves pending for a later attempt, preserves sessions on repeated channel events, and pauses tracking when stored totals are malformed. Actual DataStore code in isolated Chrome preserved invocation order for 100 concurrent writes with both cold and initialized storage, so no additional save queue was added. Account scope, unsaved totals across restart, recovery controls, and clock behavior remain open.

VoiceRejoin now preserves existing active calls, cancels pending attempts on the user's voice activity or logout, stops stale channel polling, and ignores stale persistence-cache completions. Channel and active-session state are written in one DataStore transaction. Actual code in isolated Chrome verified that an aborted transaction preserves both old values and a retry updates both. Saved targets now carry an account owner, and reconnect attempts reject foreign or ownerless records and account changes during lookup. Paired reads use one transaction; saved channel fields are validated before use. Live connection readiness, permission checks, timeout semantics, and state changes after a paired read remain open.

The proposed version is `3.0.0.0` because older scheduled data now requires explicit recovery. See [VERSIONING.md](VERSIONING.md) for the compatibility and downgrade implications. No release tag has been created for this audit.

Message pronouns now read the message channel's guild rather than the browsed channel, and subscribe to formatting, self-visibility, and account changes. All UserSettings API consumers declare their dependency explicitly. The required SupportHelper currently enables that API globally, which previously masked missing declarations.

StatusWhileActive binds saved status to its account, discards it on logout, initializes from the current call when enabled, and preserves manual status changes when leaving or stopping. The unrelated channel-status text event handler was deleted. Mocked lifecycle regressions cover these changes. Repeated in-call voice events now preserve the session record and manual presence choices. Failed applications release that record for retry; live event ordering remains unverified.

AutoDND now binds restoration to the account and the status it applied, clears saved state on logout, and restores on shutdown without overwriting manual changes. Enabling AutoDND reads already-running games from the shared store. Invisible-status exclusion no longer blocks game-exit cleanup. Both automatic status plugins propagate Flux update promises and log asynchronous lifecycle failures locally. Repeated game events no longer reapply the status during a recorded session, including when the initial status already matched. Failed applications permit retry without clearing a newer session. Failed restoration recovery remains under review.

GIF descriptions handle missing sources, exclude query parameters and fragments, and match literal GIF extensions. BetterGifPicker now marks its patch-dependent setting as requiring restart. StickerBlocker removes unused wrappers and a repeated ID, uses Discord's React export, and subscribes to display preferences without requiring restart. The block decision now subscribes to the saved list; live patch compatibility and actual client refresh remain open. SteamStatusSync skips unmapped status launches; its event payload assumptions and live protocol behavior still require validation.

TalkInReverse now has one plugin-owned send hook, preserves grapheme clusters when reversing text, and shares temporary toggle state across composers using the existing Zustand API. Its duplicate SVG was removed. The chat-button API already provides the error boundary; no extra wrapper was added. Live composer and subscription behavior remain unverified.

StreamingCodecDisabler no longer assumes all codecs are supported or carries capability values across responses. It validates complete responses before applying settings, ignores callbacks from stopped runs, logs asynchronous failures, and hides the unimplemented VP8/VP9 controls while retaining saved keys. Codec support does not prove prior enabled state, so restoration still needs native evidence.

StatusPresets saves special object-property names as ordinary entries, deletes by the stored key, and subscribes its submenu to preset and premium-state changes. Actual SettingsStore tests cover change notifications and serialization/reloading. Failed applications produce one failure toast; invalid expiration values are rejected before updating Discord. Valid expiration tests cover no expiration, timed expiration, and next local midnight across daylight saving. The internal saved-record field is hidden from plugin settings. Live custom-status payloads and remaining saved-data validation still need work.

WebContextMenus shares image downloads, reports copy/save failures, releases decoded bitmaps, and preserves URLs outside Discord media. Real browser conversion checks cover bitmap cleanup and PNG pixels; OS clipboard permissions and live menus remain unverified.

UserPFP separates remote avatars from saved local overrides, gives local choices priority, rejects malformed remote maps, and cancels stale database loads. Avatar edits use transactions before updating memory. File reads cancel on replacement, typed URLs, or unmount, and guild avatar URLs are no longer rewritten. Legacy saved entries are preserved because their provenance is unknown. Local-data validation, file constraints, and live-client behavior remain under review.

MediaPlaybackSpeed preserves a speed chosen before the first voice play event, identifies voice controls through an explicit patch prop, and falls back to 1x for invalid saved rates. Its control uses the shared button and radio menu items. CustomFolderIcons removes the redundant save helper, unused class, forwarding callbacks, repeated lookups, and non-null assertions; its renderer uses the shared error boundary. Regression fixtures cover these paths. Live patch matches, layout, and remaining settings reactivity are unverified.

Sticker menus use format metadata instead of CSS-name guesses, and sticker blocking reads subscribed settings without a duplicate cache. Expression cloning settles file-read failures, validates server errors, captures the chosen name before asynchronous work, keeps visible input and validation aligned, and subscribes its server list to account, permission, emoji, and sticker stores. Clone controls use shared keyboard-accessible buttons. Each modal owns its pending request, prevents duplicate activation, and cancels on cleanup; a two-modal fixture verifies that closing one leaves the other request running. Plugin stop and logout cancel downloads and file reads. Intentional cancellation produces no failure toast.

CopyStatusUrls awaits the shared clipboard helper and reports asynchronous failures. Its metadata result is validated before copying; malformed results leave the clipboard untouched. The broad webpack finder and patch still need live-client validation.

Updater checks now clear stale selections and reject superseded downloads before writing. Renderer checks respect resets, concurrent update requests share work, failed rebuilds remain available for retry, and recovery prompts relaunch only after success. HTTP archive replacement writes to a temporary sibling before renaming it into place. Isolated filesystem tests cover partial writes, rename failures, retries, and real ASAR contents. Native operations already in flight, actual Electron archive locking, archive integrity checks, and crash durability remain open.

RelationshipNotifier snapshots guild, group, friend, and request data before asynchronous persistence. Checks stop after account changes, logout, restart, or reconnect; old caches and manual-removal markers are cleared at session boundaries. Overlapping manual removals retain separate markers, and delayed alerts recheck current relationships and settings. Notification rejections are logged. Startup sync failures are logged, and connection setup cancels the duplicate startup timer. Guild and group removals keep storage current with alerts disabled, using one path for manual and external removals; two redundant deletion helpers were removed. Deferred-storage and lifecycle fixtures cover these changes. Real Discord event ordering, failed manual operations, and reconnect baseline ordering remain open.

PinDMs reads categories from the current account settings instead of retaining a startup array. Category modals preserve account ownership through lazy loading and reject edits to replaced categories, using the underlying SettingsStore object identity rather than transient proxy identity. Forms reject blank names, normalize cleared colors, and ignore duplicate category saves. Render callbacks are released on unmount without clearing a newer mount. The shared settings hook now compares subscription path values and snapshots paths for cleanup, avoiding resubscriptions caused only by new array objects. Fixtures use actual SettingsStore behavior with mocked React hooks; live sidebar rendering and sorting assumptions remain unverified.

VoiceMessages preserves the browser recorder MIME type, releases streams after failure or unmount, and handles repeated controls using the recorder state. Preview timers run only while recording, and preview URLs belong to committed audio blobs. Send requires completed metadata matching the current blob. Native recording paths reject invalid types before filesystem access. Focused fixtures cover these boundaries; isolated Chrome checks use synthetic audio streams. Desktop recorder lifecycle, upload format handling, account and channel ownership, and live message delivery remain open.

Transcription workers release script URLs when construction fails. Prepared-audio failures remove only their own cache entry, preserving newer retries. Copy controls use the shared clipboard feedback instead of custom state and timers, and malformed timestamp ranges are discarded while transcript text is preserved. Worker callbacks use unknown payload types, and native audio bytes retain their precise buffer type. Synthetic Chrome decoding verifies mono/stereo mixing, resampling, and context closure. Model downloads, account ownership, cache memory limits, and live Electron behavior still need further validation.

Transcription stop and logout terminate active workers and invalidate pending results before clearing caches. Result caches and component keys include the account, and jobs check current account ownership before starting and accepting delayed results. Language pickers retain their opening account, session, and job. Model URLs require HTTPS without credentials or nonstandard ports on the existing hosts; cached and downloaded responses share one path using the transferred byte length. Cache storage failures are reported, and progress percentages are finite and bounded. Fixtures cover these changes; live account event ordering, already-issued native/provider requests, model integrity, redirects, and memory limits remain open.

Translate retains the selected provider when DeepL credentials are missing or its quota is exhausted. It reports the failure instead of forwarding text to Google or resetting provider and language settings. The existing auto-translation send hook cancels sending on translation failure. Provider fixtures verify that these failures make no Google request.

Translation responses validate required fields and report provider failures without source text or response bodies. Native provider POSTs reject redirects, and unused error bodies are cancelled. Missing message views discard late results; an older view cannot remove a newer view's handler. The unused fallback language converter was deleted. Account ownership, overlapping requests, native response limits, and live provider compatibility remain open.

Translate language options follow provider changes, and only the main composer owns the enabled tooltip. Native handlers reject invalid argument types and omit raw exceptions from IPC failures. Auto-translation cancels before changing text after account changes, logout, plugin stop, disabled settings, or intervening text edits. Context-menu and popover translations share one delivery function that rejects results from previous sessions. Deferred fixtures cover these cases; already-issued requests, overlapping same-message requests, mounted accessory state, response limits, and live account-event ordering remain open.

DeepL automatic detection omits the source-language field, and source labels use the real language tables without inherited object properties. Its native handler rejects UTF-8 request bodies above the documented 128 KiB limit. Kagi stops before sending without a session token. The unused plugin-object translation export was removed. Translation accessories are keyed by account and message, and each message retains setters for all mounted views. Actual React fixtures verify state reset, duplicate-view delivery, and cleanup; Discord stores and parsing remain mocked. Request cancellation, response limits, concurrent result ordering, and live event timing remain open.

CustomRPC preserves raw numeric input, validates saved timestamps and party counts, bounds shifted timestamps, and keeps button labels paired with their destinations. URL controls parse destinations and hostnames, unset dropdowns use their declared defaults, and empty asset responses remain retryable. Deferred SDK fixtures verify overlapping settings, account changes, shutdown, cache ownership, coalescing, and the cache limit. Shared inputs have accessible labels. Live Discord behavior, saved URLs that bypass the controls, account transitions back to the original account, and remaining settings synchronization still need work.

UnitConverter now processes speed and compound measurements before their component units, uses defined conversion factors, carries rounded inches into feet, and preserves registrations across multiple message views. Unicode token boundaries prevent conversions inside identifiers and unsupported dimension suffixes. Fahrenheit accepts the same spacing as Celsius. Message/account changes and content edits reset displayed conversions; an older popover action converts each view's current text. Duplicate icons, dismiss controls, unused CSS, conversion loops, and rule types were removed or consolidated. Negative compound values, nonfinite results, URL/code handling, embedded-message behavior, and live account transitions remain open.

TranslatePlus propagates provider failures, validates Google and Toki response fields, and rejects malformed dictionaries before caching. Failed dictionary loads remain retryable. Inherited dictionary names cannot become translated text, and JSON parse failures omit response snippets. Direct branch returns replace the mutable result accumulator. The pinned dictionaries were checked for compatibility; language toggles now control their own routes. Multiple message views retain independent handlers, and account/message changes reset displayed state. The shared dismiss button replaces duplicate CSS; unknown language codes render as text. The duplicate language file and unused Sitelen regex conversion machinery were deleted; existing labels and conversion output were checked against the previous code. Superseded requests and results from a previous account are ignored. Failures show one toast instead of replacing translated text or logging raw exceptions, and unread error bodies are released. The custom provider policy, dictionary conversion semantics, response limits, cancellation, and remaining request lifecycle concerns remain open.

PermissionsViewer sorts mixed overwrites consistently and prepares a sorted copy before opening its modal. It skips empty member requests, displays unknown users, and subscribes open views to member, user, and role changes. Shared controls replace button-like divs; icon controls have accessible labels. Deleted roles retain ID copying without offering or dispatching the legacy View as Role action. Actual React fixtures cover member and role updates and subscription cleanup using controlled Discord stores. Menu actions recheck departed members and deleted guilds/channels before opening. The profile button waits for the permission module through the shared async hook; rejected loads log a generic error. Live patches, native keyboard handling, permission semantics, and remaining modal lifetime findings remain open.

TriviaAI was deleted under the plugin policy because it required a user-supplied API key and arbitrary endpoint. Its three files contained 436 source lines and had no external callers. The generated catalog, all 32 built JavaScript/map files, and both extension packages exclude the plugin. Existing stored settings are retained; no replacement provider integration or cleanup mechanism was added.

The shared webpack chunk loader retains pending and successful requests but clears failed results. A later caller can retry without a timer or retry loop. Regression fixtures exercise the actual loader and canonicalizer; a React integration check verifies that reopening PermissionsViewer after a failed chunk load succeeds and cleans up listeners. Unused loader aliases and exports were removed from PinDMs, CustomUserColors, and UserPFP.

FindReply searches the current message store when clicked. Reference matching accepts reply messages and excludes pin notices and thread starters. Direct replies within the same millisecond remain eligible; older indirect mentions remain excluded.

The navigator waits for its paginator and contains render failures through the shared error boundary. FindReply and ReviewDB now import the same paginator and loader from `@webpack/common`; FindReply no longer imports ReviewDB's modal to access those exports. Replacement reply lists reset selection before rendering controls. The duplicate stylesheet import, redundant root flag, navigation toast, manual stylesheet lifecycle, and unnecessary restart flags were removed.

Stopping releases the root and element. Closing, an empty or single-result search, and a missing destination all clear controls and their click listener. Existing roots move into replacement containers. Container selection now resolves Discord's CSS module and uses the shared selector helper, supporting hashes before the class name and composed classes. Isolated Chrome and actual React checks cover these cases, delayed loading, failure containment, and reopening. The jump action has an explicit type, and the redundant ref annotation was removed.

Direct DOM rendering, live container and patch compatibility, indirect replies within the same millisecond, updates to an open reply list, account changes, and stylesheet behavior still require review.

SVGO was an unused development dependency and was removed with fifteen transitive package versions and its release-age exception. The [executable-link advisory](https://github.com/advisories/GHSA-w27v-7q3p-w38r) and [foreignObject advisory](https://github.com/advisories/GHSA-4vpr-x523-8j87) concern its opt-in sanitization feature, which this repository did not use. Stylelint's transitive colord dependency was updated within its supported range to 2.10.0 for the [malformed-color parsing advisory](https://github.com/advisories/GHSA-2wm5-q62r-hmrv). No override or new dependency was added. The registry audit reports zero known advisories for this branch on September 12; this does not certify the codebase or close alerts on unmerged main.

FollowVoiceUser no longer attaches conditional React state to the menu that calls it. The duplicate store and channel-action lookups and repeated about notice were removed. Actual React with the shared context-menu API reproduced the previous hook-order crash and verifies friendship/account changes, toggling, stopping, and patch removal. Following now clears on logout, including account switching; the next account must explicitly choose a friend to follow. Discord menu primitives and voice actions were controlled fixtures. Live menu lifetime and voice-event ordering remain open.

Filename extension matching is case-insensitive in both filename plugins, while unmapped extension casing is preserved. Anonymizing an uppercase or mixed-case compound tar extension retains the whole extension. Random-name lengths must be integers from 1 to 255; invalid stored values fall back to seven characters, and unknown stored methods use the same bounded random path. Existing method numbers remain unchanged. Actual React with the shared number-setting component verifies validation feedback, rejected edits preserving saved values, and correction. The allocation ceiling is a plugin limit informed by [common filesystem component sizes](https://learn.microsoft.com/en-us/windows/win32/fileio/maximum-file-path-limitation), not a claimed Discord filename limit or complete portability check. Broad non-alias extension mappings remain under review because renaming alone does not establish how Discord processes the contents.

FrequentQuickSwitcher now retains the frequency entries it already read instead of discarding their values and reading settings again in every sort comparison. Its 20-result limit, result fields, and channel record identities remain intact. A controlled 30-channel test reduced frequency reads from 59 to one and verified fresh values on the next search, filtering, frozen records, and missing channel names. Live query handling, result types, selection, and incomplete frequency settings remain under review.

FriendTags no longer records an unsuccessful write as saved, allowing the same data to be written on a later attempt. Successful duplicate saves are still skipped. Its unused managed stylesheet and import were deleted after checking that the build template only registers the style and that PermissionsViewer has its own stylesheet. Initialization now validates stored records without writing defaults or replacing invalid data. The editor waits for the shared load, failed loads prevent saves, and stop invalidates delayed reads. Controlled regressions cover invalid JSON and record shapes, missing storage, read failure, and reads settling after restart. Tag-card edits now run in input handlers instead of mount effects, target the selected record directly, parse comma-separated IDs, and remove whole IDs. Controlled source and actual React fixtures verify no mount writes, editing one of two identically named tags, controlled input updates, and preserving longer IDs during removal. Tag deletion and context-menu actions now target record identity. Temporary WeakMap IDs distinguish duplicate names in menus and keep surviving editor rows mounted after deletion without changing the saved format. Controlled source and actual React checks cover targeted deletion, parent rerender, retained input drafts, distinct menu IDs, and stale removed-tag actions. Failed writes now produce a generic failure toast, remain eligible for a later save, and do not reject into event handlers. Failures settling after stop suppress stale feedback. The regression covers failure/retry, successful deduplication, and stopped-save rejection. The user list now renders each stored ID once without modifying saved records, the settings wrapper was removed, and user records use the shared Discord type. Four focused regressions, TypeScript, lint, and standalone build passed for this cleanup. External edits while settings are open and the live quick-switcher patch remain under review. This does not establish live Discord behavior.

## Verification record

Evidence applies to the recorded commit and scope, not to a future merge with main.

| Check | Commit | Result and limits |
| --- | --- | --- |
| Broader performance/correctness suite | `dcdd1f5ab` | 519 tests and timezone correctness checks passed, including 466 plugin regressions. |
| Repository-wide ESLint | `8a373e5fb` | Passed for configured source and config rules. Build output, browser output, vendored types, and test scripts are outside those rules. |
| CSS lint | `40bc540e6` | Passed with the updated colord dependency; excludes userplugins. |
| Internationalization lint | `5e869fea9` | Passed for tracked source markers and patch strings, not live Discord module compatibility. |
| Latest focused regressions and TypeScript | `8a373e5fb` | Full TypeScript and all four FriendTags regressions passed. Both tracked FindReply regressions, both FollowVoiceUser regressions, filename casing and settings cases, frequency lookup, and failed tag saves passed in the broader suite. The actual React list-replacement/boundary fixture, using the shared paginator exports, passed at `32c02f722`. All five PermissionsViewer regressions and the shared-loader regression passed in the broader suite. Source fixtures reject TypeScript syntax diagnostics before execution. |
| Standalone build | `8a373e5fb` | Passed after the quick-switcher and FriendTags changes. Release installer/parser exclusion was verified separately at `1f7dab047`. This does not establish installed-client behavior. |
| Development build | `0fc6ab63b` | Passed after composer, codec, preset, and game-status startup changes. |
| Installer review browser checks | `d7b99b737` | Actual templates and generator functions in isolated Chrome preserved literal metadata, all four native/pre-send warning combinations, the native acknowledgement gate, and cancellation. Electron IPC and real installation were not exercised. |
| Native development filter | `dc2df481a` | Actual development and reporter bundles retain the installer; release bundles exclude it. |
| Web build | `8a373e5fb` | Passed. Packed Chromium/Firefox manifest versions match `3.0.0.0`. |
| Release artifact audit | `8a373e5fb` | Passed for `dist`, including ZIP entries. This is a credential-pattern and private-runtime-path check. |
| Dependency audit and installed parser | `40bc540e6` | Frozen install passed. Stylelint resolves colord 2.10.0; three ordinary colors and two malformed 64 KiB inputs passed direct parser checks. Registry audit reported zero known advisories on September 12. |
| FrequentQuickSwitcher search | `6708f7bcf` | Actual source preserves ordering, the 20-result limit, channel record identity, and fresh reads between searches. Frozen store fixtures remain unchanged. No live quick-switcher patch was exercised. |
| FriendTags save retry | `e4b83b8c1` | Actual source retries after controlled storage rejection and skips only successfully committed duplicate data. Storage was controlled by the fixture. |
| FriendTags stylesheet removal | `df56ee934` | Both desktop renderer bundles omit the unused style registration and retain PermissionsViewer CSS. |
| FindReply container relocation | `93de7dfa1` | Isolated Chrome and actual React DOM preserve the root and component state when moving to a replacement container. Clicks continue working; zero/one-result searches and missing containers remove stale controls and their listener; resolved composed class names work with a leading hash; stop removes the element and a later action starts fresh. Discord message stores and navigator contents were controlled fixtures. |
| FollowVoiceUser lifecycle | `5d723030d` | Actual React and the shared context-menu API preserve hook order across friend/self/no-account states and patch removal. Controlled store/action tests verify logout and account switching clear following, a fresh follow works, and stop clears it. No live voice selection was performed. |
| Anonymous filename settings | `88ada37dd` | Actual React and the shared NumberSetting component preserve saved values on invalid edits and recover after correction. Actual filename code preserves method numbers and bounds allocation when the stored length or method is invalid. No upload was performed. |
| FindReply paginator loading | `e1270aaad` | Actual React and useAwaiter wait for a deferred paginator, handle false/rejected loads, and ignore completion after unmount. Closing unmounts controls and removes the document listener; a new reply list reopens with one listener; Discord modules and DOM were controlled fixtures. |
| Permission loading and retry | `a3013e6b1` | Actual React, useAwaiter, webpack loader, canonicalizer, and permission-view code recover on reopening after a rejected download and retain successful loads. Chunk downloads and Discord stores were controlled fixtures. |
| Plugin catalog and removal | `75aeb9d3a` | Generated 380 plugin entries without TriviaAI. Built JavaScript, source maps, and extension ZIP entries also exclude it. |
| PermissionsViewer React checks | `16ba9f64d` | Actual React 18.3.1 verified member-role and permission updates, member removal/return, sort action, and listener cleanup. Discord stores, popouts, and shared Clickable behavior were controlled fixtures; native client behavior remains unverified. |
| Avatar startup and edit ordering | `296e36989` | Actual loader, editor actions, and DataStore in isolated Chrome preserved edits in 20 overlapping runs, ten with startup first and ten with saving first. This checks one client context, not synchronization between clients. No extra production guard was added. |
| Avatar edit transactions | `3bf4098d6` | Actual AvatarModal actions and DataStore code in isolated Chrome preserved storage and memory after an aborted write, kept both concurrent user edits, and deleted only the selected override. React rendering and Discord dependencies were mocked. |
| Real IndexedDB queue behavior | `ce04cc40a` | Isolated Chrome verified aborted additions, ordered concurrent additions, aborted clearing, and subsequent successful clearing using actual queue and DataStore code. |
| Clone download cancellation | `1ea9bc2e2` | Actual cloning code in isolated Chromium against a local HTTP server passed stop/logout checks before response headers and during body download. All four cases closed the connection, settled without failure feedback, and made no upload call. Discord stores, upload helpers, and toasts were mocked; already-issued uploads remain unverified. |
| Clone file-read cancellation | `4c491774d` | Actual clone helper with native Chromium Blob and FileReader aborted a pending read once without uploading. A completed read removed its abort listener. Downloads, stores, and uploads were mocked. |
| Clone button keyboard behavior | `38736e4cd` | Actual CloneModal props, shared Button, and stylesheet in isolated Chrome produced 64px controls at a 16px parent font, Tab focus, a focus ring, Enter/Space activation, and no activation while disabled. React and Discord dependencies were mocked; full client layout and uploads were not exercised. |
| Playback rate application | `44d2a1156` | Actual MediaPlaybackSpeed code with Chrome audio/video elements passed 42 valid/invalid rate cases, preserved a 3x first-play selection, and verified one-shot listener and cleanup behavior. Play events were synthetic; no audio or live Discord UI was exercised. |
| Image copy conversion | `41343b959` | Actual WebContextMenus code in isolated Chrome converted a JPEG to a 3 by 2 PNG, preserved red pixels, and closed the source bitmap exactly once. Clipboard delivery was intercepted; OS clipboard permission and live Discord menus were not tested. |
| Translation dismissal keyboard behavior | `7e5ae9a39` | Actual shared TextButton props and shared/plugin CSS in isolated Chrome passed Tab focus, a visible focus ring, Enter and Space activation, and zero form submissions. React dismissal behavior was verified separately with the actual shared component. Browser event handlers were fixture counters; full Discord layout was not exercised. |
| UnitConverter accessory checks | `5f91dfd97` | Isolated React 18.3.1 verified content-edit resets and an older popover action converting the current text, plus message/account resets and logout cleanup. Earlier fixtures verified multiple views, surviving registrations, and shared dismissal. Discord stores used a controlled bridge; live client behavior remains unverified. |
| TranslatePlus accessory checks | `2bfa43606` | Actual React 18.3.1 fixtures verified multiple views, cleanup, message/account replacement, logout, stale completion after remount, shared dismissal, unknown language labels, and proxy-backed embedded-message exclusion. Providers and Discord stores were mocked; live event ordering and request cancellation remain unverified. |
| Translation accessory reconciliation | `bd695428d` | Actual plugin rendering and accessory code with isolated React 18.3.1 and its test renderer reset state on message/account changes, unmounted on logout, delivered to two views of one message, and preserved the remaining view after cleanup. Discord stores used a controlled external-store bridge; parsing and icons were mocked. No live Discord client or provider was exercised. |
| Native translation redirects | `638d668e6` | Actual native handlers with Node fetch rejected local 301, 302, 303, 307, and 308 redirects. Ten valid initial requests reached the server; none reached the redirect destination. Failure responses contained no exception details. Credentials and text were synthetic; remote provider compatibility and Electron IPC were not exercised. |
| Transcription model response transfer | `28bf744a0` | Actual worker wrapper with native Node fetch decoded a local 66-byte gzip response into 2,030 bytes. Download and cache-hit responses reported the decoded size and preserved every byte; native structured-clone transfers detached both sending buffers. Worker delivery and persistent storage were represented by a transfer sink and cloned in-memory cache. Remote models, IndexedDB, and Electron were not exercised. |
| Transcription audio decoding | `d7c9fcdf9` | Actual decoder in isolated Chrome passed mono/stereo WAV cases at 16 kHz and 48 kHz, producing 16,000 samples per second and the expected channel average. All five native AudioContexts reached closed state, including invalid-input failure. Audio was synthetic; Electron integration, long-file memory limits, and transcription models were not exercised. |
| Video preview decoding | `fd216ca34` | Actual preview code in isolated Chrome produced a PNG from an FFmpeg-generated WebM and returned null for invalid video. |
| Theme replacement on the filesystem | `07903f31c` | Actual native code preserves installed files after injected partial writes and rename failures, and cleans temporary files. This is not crash-durability testing. |
| Theme provider download | `9b03d446d` | The actual native handler downloaded catalog entry 91 with HTTP 200 and no redirect into an isolated directory. All 136 catalog entries were separately checked against ID, filename, and catalog-content size rules. |
| HTTP archive replacement | `0ea68f8c4` | Actual updater code on Windows preserved old ASAR bytes and readable contents after injected partial-write and rename failures. Retrying installed the complete new archive and removed temporary output. Electron original-fs was represented by Node fs; live Electron locks and crash durability were not tested. |
| Extension archive extraction | `e33811df0` | Actual extraction code, fflate ZIP decoding, and the Windows filesystem preserved nested files/directories, skipped metadata, rejected three traversal/absolute paths, removed partial output, and preserved an outside sentinel. Electron loading, archive resource limits, and crash durability were not tested. |
| Extension install limits | `d26010040` + `fc00872cd` | The actual installer passed 19 focused cases covering bounded streaming downloads, HTTP failures and cancellation, metadata-only ZIP preflight before inflation, entry and expanded-size limits, path checks, declared-size mismatch cleanup, both Electron loading APIs, real temporary-filesystem cleanup, staged extraction, and failed promotion cleanup. Fresh installs now write to a sibling staging directory and rename it into the cache only after extraction completes, so an interrupted extraction does not leave the final cache path present. Full TypeScript and standalone also pass with this change. Live Electron loading and power-loss behavior during filesystem rename remain unverified. |
| Uninstall button browser checks | `d4efc449d` | Actual shared component props and styles in isolated Chrome verified the accessible name, keyboard focus indicator, and consistent 32px sizing in both stylesheet orders. Full Discord layout was not exercised. |
| Other isolated browser checks | Earlier audit commits | Specific sticker-storage transactions, codec conversion, and CSS behavior were exercised. These are not general live-client acceptance. |

Mocked Discord requests do not establish live account-switch, message-send, or plugin-patch compatibility. No real Discord messages were sent by these regression fixtures. At `68f966486`, GitHub reported an empty check rollup for the open draft PR, no auto-merge request, and conflicts with main. Current-head CI success has not been established.

## Remaining work

The full finding ledger is still being worked through. This list identifies major open areas and is not an assertion that other findings are closed:

- Scheduled-message legacy recovery, remaining persisted-data constraints, media cancellation, and coordination between separate client contexts.
- Remaining plugin account, cancellation, network-response, storage, and resource-lifecycle findings across the broader codebase.
- Runtime validation of patch anchors. The current patch validator reports 187 warnings; those warnings have not all been resolved or justified against current Discord modules. The latest source passes removed the sole replacement-level try/catch warning and refreshed FriendCodes, MoreUserTags, FakeNitro, RPCEditor, NewGuildSettings, OnePingPerDM, Timezones, ClientSideBlock, StatusPresets, and MessageLoggerEnhanced against current upstream module shapes. Broader behavioral rewrites remain separate review work. Remaining regex warnings are being reviewed against current code rather than mechanically bounded.
- Native and provider behavior that mocked tests cannot establish, including target-platform installer and live-client acceptance.
- Final release checks and live acceptance against the combined source. Main at `8fc182ba7` has been integrated and its release history preserved.
- A final finding-by-finding disposition, current-head CI, and final audit report before removing draft status.

The audit must not be described as a clean-codebase or release-readiness certification while these items remain unresolved.

## September 24 continuation

PR #49 was reviewed in full. It upgrades SVGO and its lockfile dependencies; #47 already removes unused SVGO in `1923b0120`, so no changes were imported. PR #49 was closed and its Dependabot branch deleted.

IgnoreActivities now uses the Registered Games overflow context menu from upstream commit `09d9f16f9`, removing the obsolete row patch and renderer. The existing toggle path still refreshes the ID cache and activities. It does not change Discord game-detection preferences. BetterFolders already has a deterministic expand/close implementation, so the upstream pre-toggle-state change was not imported. Live Discord menu compatibility remains unverified.

Validation for the IgnoreActivities change: full TypeScript, focused ESLint, and `git diff --check` passed. Patch lint reports zero errors and 187 warnings.

Main integration on September 24 preserved the audit changes while bringing in main at `8fc182ba7`. Only version and changelog conflicts required manual resolution. Automation tests, full TypeScript, and the standalone build pass on the combined source. These checks do not establish live Discord behavior.

The combined-source performance suite passes all 520 tests and timezone correctness checks. Focused automation ESLint, repository CSS lint, and staged/working-tree whitespace checks pass.

The integrated automation output lookup crashed on an unknown saved wait-event type before the editor could show its validation error. Output discovery now returns no fields for unsupported events, and the event form retains its selector with a correction prompt. A regression reproduced the exception before the fix and now verifies variable/output discovery and preservation of the saved value. No workflow was run against Discord.

StatusPresets current-status icon now reads `emojiId` and `emojiName`, matching its settings write path and ProfileSets, rather than the unrelated `emoji` object shape. Actual render-prop fixtures reproduced the missing icon and verify Unicode, custom, and absent emoji cases after correction. This checks payload handling, not a live Discord menu.

VoiceStats now ends active sessions and clears its save timer on logout by reusing its existing channel-stop function. The source regression reproduced sessions surviving logout, then verified accrued time is retained, logged-out time is excluded, and a later session can begin. All eight VoiceStats regressions pass. Account-specific storage and live Discord event ordering remain open.

VoiceStats session timing now uses the monotonic performance clock. A controlled-clock regression reproduced 10,800 stored seconds for 91 elapsed seconds under forward wall-clock adjustments; it now preserves the elapsed duration and fractional carry, tolerates backward wall-clock changes, and counts a zero-origin session. Session origins are memory-only, so saved totals need no migration. System sleep and live client behavior remain unverified.

VoiceRejoin rejects saved timestamps ahead of the current clock instead of treating them as unexpired. Controlled timestamp tests cover a future value, zero elapsed time, the exact configured timeout, and one millisecond beyond it. All nine VoiceRejoin regressions pass. This does not resolve live permission/readiness checks or the distinction between last saved activity and disconnect time.

ProfileSets now returns its custom-status update promise through `loadPresetAsPending`; the existing manager catches load failures and shows one failure toast. A deferred-update regression reproduced premature completion and now verifies the loader stays pending and propagates rejection. Other profile edits can already be staged when a status update fails; no rollback or atomic profile-update claim is made. Account transitions, overlapping loads, and image requests remain open.

ProfileSets preset loading now captures the signed-in account before profile/image preparation and checks it again before staging edits or starting a status update. The source regression reproduces the previous completion after an account switch and verifies zero additional staged edits or status requests after switching accounts or logging out. Already-issued image/status requests and same-account overlapping loads remain separate findings.

ProfileSets random selection no longer builds an unused selection object or retries the same random index up to five times. It draws once from the alternatives to the previous valid index. The actual manager callback fixture reproduces repeated selection with the old loop and verifies different successive choices, one draw per choice, shrinking to one preset, and an empty list. React hooks and preset loading are controlled in this fixture.

ProfileSets save controls now clear the saving flag after failed profile preparation, keep the entered name, and show failure feedback. The actual callback fixture reproduces rejection before the fix and verifies recovery and a subsequent successful callback. This does not fix storage errors swallowed by `savePresetsData`, premature in-memory mutation, account-bound saving, or component unmount handling. Those remain open.

ProfileSets storage now discards reads completed under a different account and only saves to the account and section whose load completed successfully. Starting a load clears the old list and disables saving until that load completes. The source regression reproduced a stale account read and verifies pending-load, account, and section save boundaries. All 524 performance regressions, timezone correctness checks, and full TypeScript pass. Source ESLint passes; the regression script is excluded by the ESLint configuration and is checked by TypeScript and execution. Legacy migration ownership, swallowed write failures, and asynchronous profile preparation before list mutation remain open.

ProfileSets save preparation now retains its initiating account and list reference and rejects completion if either changed before insertion. The source regression failed before the fix and passes for account switch, logout, list replacement, and an unchanged successful save. All four focused ProfileSets regressions pass. This does not make persistence transactional or cancel image requests already issued during preparation.

ProfileSets now shares its identical avatar-decoration and nameplate equality implementation. Removed unused guild arguments from update, delete, move, rename, and import actions and their callers; those actions persist by account and section, and never used the supplied guild. Existing focused ProfileSets tests pass. This is a behavior-preserving deletion and does not establish live profile compatibility.

ProfileSets imports now retain the account and preset list that opened the picker, checking both after file reading and after the import decision. The source regression reproduced a foreign-account write and now verifies changes during reading, account changes during confirmation, list replacement during confirmation, and successful override. All five focused ProfileSets tests pass. Imported-data validation, persistence error propagation, and concurrent mutations within the same list remain separate findings.

ProfileSets import checks the list envelope and required render fields before confirmation or replacement: each entry must be an object with a string name and a representable numeric timestamp. The extended import regression reproduced acceptance of null entries, and verifies rejection of null, numeric names, invalid timestamps, and non-array documents without writes or list replacement. Optional profile fields and existing stored records still need separate validation; this check does not establish complete schema validation.

September 24 verification at 39821b223: all 526 performance tests and timezone correctness checks pass. GitHub reports two open dependency alerts on the default branch, both for SVGO 4.0.0; the audit branch manifest and lockfile contain no SVGO dependency. The alerts remain pending merge, and this targeted check is not a full dependency advisory refresh.

ProfileSets Update no longer loops through individual fields and writes the complete list for each field. The action prepares once, checks its original account, list, and target object, then updates and saves once. The menu catches preparation failures. Controlled source tests verify one write, retained name and null-field behavior, safe reordering, rejection after removal or list replacement, and rejection after account changes. Six focused ProfileSets tests pass. Persistence failures are still swallowed by the storage layer; this change does not claim transactional durability.

ProfileSets legacy migration previously deleted both legacy keys after copying only the preferred dataset. It now deletes only the copied source. A regression reproduced loss of the separate global dataset and verifies that successful scoped migration preserves it, while a failed destination write preserves both sources. Seven focused ProfileSets tests pass. The ownerless global fallback and migration coordination between clients remain unresolved.

ProfileSets now distinguishes a missing destination record from a present non-array value. Invalid destination records fail loading and remain unwritable instead of triggering legacy migration or empty-list replacement. The regression reproduced writes over invalid data and verifies no writes or deletions for null, false, zero, string, and object values in both main and server sections. Eight focused ProfileSets tests pass. Invalid entries inside arrays and visible recovery controls remain unresolved.

ProfileSets export now delegates to the existing saveFile utility, deleting its detached anchor and immediate object-URL revocation. JSON formatting, MIME type, and section/timestamp filename are unchanged. The shared utility attaches the anchor and defers cleanup. Eight existing focused ProfileSets tests pass after updating their dependency mocks; those tests do not exercise a real download dialog.

ProfileSets import now awaits the shared chooseFile utility instead of creating a detached input and returning before its change callback runs. The existing scope and malformed-file fixtures now await the full import operation, and an added cancellation case verifies no writes, refreshes, or failure toasts. Eight focused ProfileSets tests pass. Actual native picker behavior is not established by these fixtures.

ProfileSets storage maintained a selected index that no production consumer read. Removed the variable, setter, reset, deletion adjustments, and manager write. The UI retains its own selection state. The existing random-choice regression now observes the preset passed to the loader instead of the removed setter, and all eight focused ProfileSets tests pass. UI selection behavior after reordering remains a separate finding.

Both ProfileSets patch entry points now render the same module-level ErrorBoundary.wrap component with noop enabled. Previously, the manager rendered directly inside Discord settings without a boundary. This follows the existing shared component contract; event-handler failures and imported/stored-data validation remain separate concerns. No live Discord render failure was induced.

The ProfileSets manager now subscribes to UserStore account identity and includes it in the existing preset-load effect dependencies. Previously, only guild or section changes triggered a reload. The controlled hook fixture verifies account IDs change the load dependencies. This does not establish live store event ordering or eliminate the render-before-effect window.

Removed ProfileSets startup loading of the main preset section. The manager already loads the requested section on mount and on section, guild, or account changes, and the startup result has no separate consumer. Storage reads and migration now begin when the panel loads its section. Eight existing focused tests pass; this does not address stale first-render contents or coordination between multiple mounted managers.

ProfileSets import confirmation now resolves cancellation through openModal onCloseCallback, covering dismissal outside the explicit Cancel action. Explicit decisions resolve before close and retain their result. The manager fixture invokes the close callback and verifies the pending prompt resolves as cancel. Removed the unrelated ConfirmModal component and props interface after finding no consumers. Live Escape/backdrop routing remains unverified.

Reviewed all ProfileSets stylesheet selectors against their component uses. The control relocation in d1ce2d0e2 left consecutive bottom separators; removed the conditional duplicates and retained one section separator. Removed the standalone server max-width rule because the same element always has the base section class with that exact width. Other selectors remain in use. Live visual layout has not been checked.

ProfileSets search now resets page state and its visible input to one when the query changes. Page one remains a valid reset when the list is empty, allowing the existing deletion reset path to work as well. The source callback regression reproduced a later page surviving a search change and verifies both page values reset after no matches. Nine focused ProfileSets tests pass; live layout and broader selection behavior remain open.

ProfileSets reconciliation at 20e5b191d records hashes for all eight plugin files and six unresolved finding groups. The next priority is persistence: mutations precede storage and save failures return normally. Remaining groups are ownerless legacy recovery, stored/imported schema validation, bounded and cancellable image preparation, account/section ownership of the shared list, and live UI/patch compatibility. The internal record is .git/audit/profile-dispositions-20260924.json. None of these groups is closed by the recent focused fixes.

ProfileSets mutations now construct replacement arrays and publish them only after DataStore.set succeeds. Removed the five imperative storage mutation helpers. Save failures propagate to existing save/update/import feedback and new shared rename/move/delete feedback. A pending write rejects overlapping mutations; completion after account or load-generation changes does not publish into the current list. Integrated action/storage fixtures cover failed save, refresh, delete, reorder, rename, and import, plus delayed publication, overlap rejection, retry, and account changes. All 532 performance regressions and timezone checks pass. Coordination with concurrent loads, other clients, and visible recovery from invalid saved records remains open; this is not a cross-client transaction guarantee.

ProfileSets loads now wait for a pending mutation write to settle before reading storage. Replaced the write boolean with its settlement promise; the original write promise still propagates failure to the caller. A controlled interleaving regression reproduced the early read and verifies reload reads committed data after success or retained data after failure. Twelve focused ProfileSets tests pass. Separate-client and overlapping legacy-migration coordination remain outside this fix.

ProfileSets image preparation now rejects failed URL or final CDN downloads instead of returning the no-image sentinel. The regression reproduced successful completion on HTTP failure and verifies avatar/banner URL and hash paths reject, while absent images and embedded data still resolve. The existing successful guild-to-global fallback regression passes. All fourteen focused profile tests pass. Download bounds, cancellation, and the correctness of guild fallback selection remain separate findings.

ProfileSets image requests now pass a 30-second AbortSignal timeout to fetch, covering network and response-body consumption. The existing fallback fixture verifies both attempts receive the timeout signal; all fourteen focused profile tests pass. This is a per-request timeout, not an end-to-end deadline. No wall-clock timeout integration test was run. There is no existing shared bounded renderer downloader in src/utils; streaming byte bounds, conversion cancellation, and lifecycle cancellation remain open.

ProfileSets image downloads now count streamed bytes, cancel above a local 10 MiB per-image limit, and release the reader lock. Non-success HTTP response bodies are cancelled. Conversion starts only after a bounded response completes. Real Response/ReadableStream fixtures verify exact-limit acceptance and one-byte-over cancellation despite an incorrect Content-Length, without FileReader conversion on overflow. Fifteen focused profile tests pass. The cap is a plugin resource limit, not a claim about Discord upload limits. Embedded data, aggregate storage size, lifecycle cancellation, and conversion cancellation remain separate concerns.

ProfileSets storage load failures now reject after preserving the stored data and clearing the active in-memory scope. The manager catches failures, notifies only while mounted, and refreshes cleared state instead of leaving a stale rendered list. A controlled effect fixture checks mounted and closed panels; thirteen focused preset tests pass, including invalid-record and failed-migration propagation. The initial fixture wait incorrectly used the imported promise timer as a callback API; it was corrected to await setImmediate directly. Explicit data recovery controls and live rendering remain open.

ProfileSets import, current storage loading, and legacy migration now use one shared validator for the declared preset shape. It checks required fields, scalar types, finite numbers, color arrays, collectible records, custom status, and display-name styles including optional camel-case aliases when present. Invalid stored entries fail without writes or deletion. Tests cover twenty malformed field variants, non-record entries, valid minimal/full shapes, and preservation of invalid arrays in both sections. All 537 performance regressions, timezone checks, full TypeScript, and source lint pass. The new validation.ts is shared by two modules; the tracked-file inventory increases to 1,657. Discord semantic limits, effect animation internals, ownerless legacy recovery, and older nonconforming formats remain unresolved; such stored records are retained rather than rewritten.

Legacy ProfileSets selection now distinguishes absent records from malformed records before choosing a migration source. Previously, malformed account-specific values were bypassed in favor of a global array. A regression reproduced that fallback and now verifies null, false, string, and object records reject without writes or deletes and keep saving disabled. Existing legacy preservation tests pass. Automatic ownership assignment when only a valid global dataset exists remains unresolved.

ProfileSets cross-client storage review reproduced a lost update at 6dfacde25. Two independently evaluated storage modules share a clone-on-read/write store and load the same list. The first appends a preset and saves successfully; the second appends from its stale list and also succeeds, deleting the first client's addition from storage. This verifies the source-level race, not real browser coordination. DataStore.update already performs a read and write in one transaction, so the fix should use that API with stale-snapshot detection or operation-based updates. Legacy migration destination writes need the same review. No concurrency fix is claimed yet. The current full suite passes all 538 tests and timezone checks; these tests did not cover this two-client loss. Build and test, CodeQL, and dependency review passed on 6dfacde25; Windows smoke remained pending when checked. The repository audit remains incomplete.

ProfileSets ordinary saves now compare the last loaded or successfully saved list inside DataStore.update before replacing storage. The saved snapshot distinguishes an absent key from an empty list. A changed or deleted record rejects without overwriting the other client's data; reopening reloads the current list. A two-module regression reproduces the missing rejection on the previous source and verifies competing saves, initially absent/empty/populated records, deletion, and reload recovery. The fixture serializes updates and clones reads/writes; it does not claim live browser or Discord coverage. Shared lodash equality avoids a new comparator or storage format. Legacy migration still uses separate writes and remains open.

Validation for the ProfileSets stale-save fix: all 539 performance tests and timezone checks pass, along with full TypeScript and focused source ESLint. The before-fix regression fails with Missing expected rejection, rather than a fixture API error. Conflict errors currently reach existing generic failure toasts; clearer recovery feedback and migration coordination still need review.

ProfileSets migration now checks its destination within DataStore.update. A destination created after the initial read takes precedence and is validated before publication; invalid destinations reject without replacement. The regression reproduced a newer preset being replaced by the legacy preset on the previous source, then passes with the fix. The separate legacy deletion was removed, retaining a backup and avoiding deletion of source changes made during the copy. Seventeen focused profile tests, full TypeScript, and focused ESLint pass. This does not resolve ownerless dataset assignment, provide a live multi-client browser test, or make the legacy source read and destination write one transaction. The retained backup remains ignored while a destination exists.

ProfileSets mutation menus now require their rendered list to remain current before executing a change. A controlled menu regression reproduced three stale delete/move callbacks executing after replacement, then verifies all three reject and an action against the original current list still executes. Rename state now holds the preset object instead of its numeric index, preventing a replacement row from inheriting rename mode. Eighteen focused profile tests and source ESLint pass. This does not resolve all selection/loading indexes, keyboard access, or shared-panel ownership; those remain open.

ProfileSets save pagination no longer validates a newly created page against the previous render's page count. After persistence, the handler sets both page state and its input from the new list length. A regression reproduced staying on page one when saving the sixth preset, and now passes for initial counts zero, four, five, and ten. All nineteen focused profile tests, full TypeScript, and focused source lint pass. Search-mode concurrent changes, deletion/import page clamping, and live rendering remain separate review items.

ProfileSets now corrects out-of-range pages from the filtered page count after rendering any changed list. This replaces the deletion-only callback that counted all presets and was bypassed by imports. The regression reproduces page three remaining selected when the list has two pages, or one visible search page. Both page state and input now recover. Two older fixtures were updated to identify the account-load effect independently of the new pagination effect. All twenty focused profile tests pass, with TypeScript and source lint passing. Actual browser rendering and input behavior remain unverified.

Removed ProfileSets' single-use image-input predicate and one-element array search. Direct narrowing retains the existing string/object return behavior and removes an unnecessary type assertion. The broad suite caught two unrelated fixture edits from the previous pagination commit: an overbroad replacement added the preset effect filter to theme and narrator tests. Both fixtures were restored. The rerun passes all 543 tests and timezone checks; focused source lint and TypeScript pass. This corrects the test regression and does not claim live UI acceptance.

Repository reconciliation at 791813b3c confirmed origin/main remains 8fc182ba7 and is an ancestor of this branch. Published nightly and origin/nightly remain 6e664e03. Current patch lint reports zero errors and 187 warnings. Inventory is 1,657 tracked files, with thirteen additions, seventeen deletions, and 508 modifications from the nightly baseline. These counts describe scope, not completion. Current-head GitHub build, Windows smoke, CodeQL, and dependency checks were pending when inspected. Remaining finding disposition and live acceptance are still required before release.

TidalEmbeds URL recognition now requires the same numeric album/track path that its player renderer consumes. Removed four prefix comparisons that accepted missing IDs, nonnumeric IDs, and lookalike path prefixes, causing the original embed to disappear without a replacement. The regression fails on the old predicate and passes for six invalid cases and sixteen supported path/suffix combinations. Focused lint and TypeScript pass. The unbounded Discord patch remains unchanged pending bundle evidence; this source behavior fix does not prove the live patch or iframe presentation.

TidalEmbeds API review confirmed this repository's renderMessageAccessory contract is a function, PluginManager binds it to the plugin and automatically enables MessageAccessoriesAPI, and the shared API renders it within ErrorBoundary. Removed only the redundant MessageAccessoriesAPI declaration. MessageUpdaterAPI remains because its patch changes message-reference handling in Discord's accessory rerender check; removing it without runtime evidence would assume equivalent behavior. The existing actual PluginManager fixture passes auto-enablement and start/stop registration, and focused ESLint passes. No duplicate wrapper or new abstraction was added.

TidalEmbeds now renders all recognized embeds rather than only the first, matching the patch's per-embed suppression behavior. A source-render regression reproduced a missing second player and now verifies track and album players, non-TIDAL exclusion, stable embed-ID keys, and null for an empty list. Message props are explicitly typed. Both focused Tidal regressions and source lint pass; live patch matching, iframe layout, and provider playback remain unverified.

CopyStatusUrls now uses the shared showToast helper instead of constructing its own toast object and ID. The existing helper accepts the same failure type and top position, so feedback is preserved. Existing source regressions pass for delayed clipboard completion, clipboard rejection, and malformed metadata; the fixture checks toast type and position explicitly. The broad patch match remains unchanged because its correct bound still requires Discord bundle evidence.

BannersEverywhere no longer persists Discord banner URLs independently of UserProfileStore. A regression reproduced returning an old banner after the profile banner was cleared; it now returns no banner for cleared or unloaded profiles. Removed the DataStore import, cache object/key, asynchronous startup read, persistence timer, queued writes, and shutdown write. The existing PNG conversion cache is separate and unchanged. Old stored cache data is left untouched but no longer read. This intentionally stops showing historical banners for unloaded profiles. The focused regression and lint pass; store subscription, image conversion lifecycle, raw DOM replacement, and live patches remain open.

BannersEverywhere conversion now resolves with the original URL on image-load failure, unavailable canvas context, draw errors, or encoding errors. This preserves the displayed image instead of replacing it with an empty URL and settles exceptions from asynchronous onload callbacks. Removed the argument-free catch call, which did not handle rejections. Controlled image/canvas fixtures cover these four failure modes and successful conversion. Cancellation, timeouts, failed conversion cache retention, and stale DOM updates remain open; no live browser conversion claim.

BannersEverywhere no longer caches failed conversions indefinitely. Failure removes only the same cached promise, preserving a newer request after cache clearing. The regression reproduced a retry reusing the failed result, then verifies a fresh retry, successful reuse, and an old failure leaving a newer conversion cached. Cancellation of outstanding image work and stale DOM publication remain unresolved.

BannersEverywhere now renders static conversions through a URL-keyed React component using the existing useAwaiter hook and ErrorBoundary. Removed document.getElementById, imperative src assignment, and duplicate user-based image IDs. A fixture loads the actual shared hook and verifies cleanup suppresses an old URL result while the current URL updates; initial rendering uses that URL as fallback. Four focused banner regressions pass. This establishes callback ownership in controlled React-effect fixtures, not actual Discord rendering, plugin-stop unmount behavior, or image-request cancellation.

BannersEverywhere now tracks pending conversion cleanup callbacks separately from cached results. Stop settles each pending conversion with its original URL, clears image handlers, removes src, and clears the cache. Successful and failed completion remove their pending callback; a captured late load callback checks ownership before doing canvas work. Five focused banner tests pass, including two pending images, repeated stop, late callback suppression, retries, and successful caching. Real browser network cancellation from removing src is not claimed. Timeouts and resource bounds remain open.

BannersEverywhere conversions now use the existing cancellation path after 30 seconds. Completion clears the timer, and timeout settles with the original URL, clears handlers, removes src, and releases pending/cache ownership. The controlled timer fixture covers both timeout and stop with two requests and verifies no timers remain after settlement. Five focused banner tests pass. This is a local timeout, not a wall-clock browser proof, and cannot interrupt synchronous canvas work; image/canvas size and active-work bounds remain open.

BannersEverywhere now uses the existing IconUtils.getUserBannerURL export instead of assembling Discord CDN paths and extensions. It requests size 1024 with animation available to the existing animation-selection path. The source fixture verifies the user ID, current banner hash, animation option, and size passed to the helper, while cleared/unloaded profiles still return no banner. Five focused banner tests pass. This validates integration with the declared shared API, not the current live Discord implementation. USRBG conversion and remaining resource/subscription findings are unchanged.

BannersEverywhere no longer rewrites every .gif URL to .png before conversion. The source fixture reproduced a third-party GIF URL being changed to an unverified PNG location. Discord format selection now passes the animation setting to IconUtils; third-party URLs remain intact for canvas conversion. Tests verify both animation values reach the helper and the original GIF URL remains the component key/fallback/conversion source. Five focused banner regressions pass. Settings subscriptions and actual provider/browser behavior remain open.

BannersEverywhere conversion canvas dimensions are capped at 1024 while preserving aspect ratio and avoiding upscaling. The previous source allocated an 8192 by 4096 canvas in the regression; it now uses 1024 by 512. Tall and already-small cases also pass, and drawImage receives the output dimensions. Six focused banner tests and source lint pass. The cap limits canvas/output allocation, not image transport or initial browser decoding. Active conversion concurrency and aggregate encoded-cache size remain separate limits to assess.

BannersEverywhere cache eviction now calls the existing cancellation function for the evicted URL. Pending callbacks are keyed by URL, with ownership checks so a stale event cannot remove a newer same-URL conversion. The regression starts 101 conversions, verifies the oldest settles and only 100 remain owned, retries the evicted URL, invokes an old error callback, and verifies shutdown settles all remaining requests. Seven focused banner tests pass. The 100-entry policy now bounds plugin-owned pending work as well as cached entries; it is not a claim about browser transport termination or aggregate memory bounds.

USRBG feed loading now validates string endpoint/bucket/prefix fields and an object of string user entries before publishing data. Invalid responses log a generic warning without response contents and leave existing data untouched. The live feed returned HTTP 200 with these field types and 207,301 string-valued user entries when inspected; no individual user entries were logged. Source fixtures cover invalid roots, missing fields, null/array user maps, invalid entry/endpoint types, and a valid response. Startup ownership, cancellation, response bounds, URL semantics, and broader patch/runtime behavior remain open.

USRBG startup now owns an AbortController. Restart aborts its predecessor; stop aborts pending loading and clears data. Checks after fetch and JSON completion prevent cancelled responses from publishing, and cleanup only clears its own controller. Fetch/JSON errors are caught with a generic warning unless cancellation was intentional. Deferred-body source fixtures verify both stop and a newer successful start prevent old data publication, with no cancellation warning. Feed timeout, response size, endpoint/path semantics, and live provider integration remain open.

USRBG startup now aborts its request after 30 seconds and clears the timer in finally. A controlled pending fetch rejects on the real AbortSignal event; the fixture verifies settlement, no data publication, cleared ownership, and timer removal. All three focused USRBG tests and source lint pass. The timeout is tested through a controlled timer rather than a real 30-second network stall. Streaming response bounds and endpoint semantics remain unresolved.

USRBG banner URL generation now narrows its local data snapshot and reads the user entry once, removing the repeated lookup and non-null assertion. Existing feed fixtures also verify valid URL output and missing-user/null-data results. Three focused tests and source lint pass. This preserves the current URL format; provider endpoint and path validation remain open.

USRBG now accepts only the configured HTTPS API origin as its feed image endpoint, matching the existing CSP host entry. A fresh live feed returned that exact endpoint, bucket usrbg and prefix v2/, with 207,301 string entries; individual entries were not logged. The existing malformed-feed regression now rejects HTTP, unrelated and lookalike hosts, credential-shaped URLs, data URLs and query-bearing endpoints. Three focused tests, TypeScript and source lint pass. A provider host migration requires an explicit code/CSP update. Path components, response bounds and live rendering remain open.


At b9c1973bc the complete performance/regression suite passed 555 tests plus timezone checks (.git/audit/usrbg-full-regression.log). This is source regression evidence, not live Discord acceptance.

ProfileSets rows now pass the rendered preset object instead of its former array index. The manager rejects objects absent from the current list before applying or selecting them, and selected state stores the object so reordering cannot move its highlight to another preset. A controlled manager regression reorders the list and confirms the original object loads, then replaces the list and confirms no additional load or selection occurs and one failure notification appears. All 21 focused profile tests, TypeScript and component lint pass. Cross-panel account/section ownership, stable row keys, legacy ownership and live rendering remain open.


ProfileSets now creates storage and action instances per mounted account/section panel instead of exporting one global active list. Its existing migration, failure-publication, generation, pending-save and transactional comparison behavior stays inside each instance. Panel cleanup invalidates pending reads and clears that instance. Actions, row mutations, loads and exports share the owning instance; main and server panels no longer replace each other. The new same-module regression resolves simultaneous section loads in reverse order, verifies both export contents and filenames, saves one without touching the other, and rejects a late read after unload. All 22 focused profile tests, full 557 regressions plus timezone checks, source lint and both standalone builds pass. Live Discord mounting and rendering, stale account callbacks, same-section refresh feedback and legacy ownership remain separate acceptance items.


ProfileSets load and export callbacks now check the storage instance against the currently signed-in account and requested section. Saving reuses the same check. Retained callbacks can no longer apply an old-account preset or export an old-account/wrong-section list before React effect cleanup. Existing regressions now cover a retained preset object with an invalid scope, export after account switch and logout, and wrong-section export. All 22 focused tests and source lint pass. This does not establish live account-switch timing or resolve legacy ownerless migration.


ProfileSets save completion now checks that its storage instance still owns the panel and matches the active account/section before updating draft text, pagination, save state or failure feedback. Guild changes create a new instance; loading a new scope resets its saving state. Saves cannot start from an unloaded scope. The delayed-save regression covers success/failure after replacement or cleanup, plus a retained closed-panel save callback. All 23 focused profile tests pass; TypeScript and component lint also pass on the final source. Live rendering remains unverified.


ProfileSets pagination now uses the already-imported shared TextInput with an accessible Page label and its value-based change callback. Removed bespoke page input colors/border/padding and duplicate name/search input overrides that suppressed component borders, outlines and shadows; only layout widths remain. TypeScript, component ESLint and focused stylelint pass. No new regression test was added for this component substitution. Current live theme/focus/width appearance still requires visual acceptance.


ProfileSets row keys now follow preset object identity through a WeakMap instead of array positions. Keys survive reordering and removal without changing stored data or adding identifiers to imported/exported presets. Distinct records with identical names and timestamps receive different keys; replaced objects receive new keys. The source-render regression covers those cases and confirms selection/click targeting remains attached to the original object. All 24 focused profile tests and component lint pass. Browser reconciliation/focus still needs live visual acceptance.
